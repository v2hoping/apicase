# apicase 概览

> 本目录（`docs/0.latest/`）是项目**当前全局最新的全部文档**，事实来源。
> 需求落地后将要点合并至此，保持其始终反映最新状态。

## 是什么

apicase 是一款 **API 接口调试、管理与用例编排** 的桌面软件。核心抽象：把「用例（case）」建模为 **DAG**——单个 API 调试 = 只有一个节点的 DAG，单 API 与编排用例在模型上统一。

设计上走 **文件即数据（local-first）** 路线：打开一个目录即一个**工作空间（workspace）**，folder 即分组，case 即用例。参考标杆 **Bruno**（理念）与 **Postman**（UI / 功能）。

## 当前能力（v0.1）

**脚手架 + 单 API 调试 MVP** —— 已落地：
- Tauri 2 桌面应用骨架可编译运行。
- 界面发起一个 HTTP 请求 → 由 Rust 后端（`reqwest`）发出 → 展示响应（状态码 / 耗时 / 大小 / 响应头 / 响应体）。即「单节点 DAG」的端到端执行。
- Postman 风格 UI：方法下拉与 URL **连体** + Send 请求行；Headers / Body 配置 Tabs；响应区 Body(Pretty/Raw) / Headers Tabs。
- 错误处理：空 URL、非法方法、网络失败均有可读提示。

**工作空间与界面外壳** —— 已落地（2026-06-30）：
- 左上角「选择工作空间」自定义下拉：打开/创建工作空间（选目录 → 幂等写 `application.yml`）、最近列表（内存态）。
- 左侧**文件树**：选中工作空间后懒加载、可展开/折叠目录；左侧栏可拖动调宽；空状态可点击「创建或打开」。
- macOS **自定义标题栏**：隐藏标题、红绿灯下移与下拉垂直对齐、顶栏可拖动窗口；全屏时下拉贴左、退出全屏平滑让位不覆盖。

**尚未实现**（见路线）：点击 case 的解析渲染、读写 `.yml` case、最近列表持久化、文件树自动刷新、多节点 DAG 编排、environment 多套配置、历史、鉴权、导入导出、OpenAPI(SPEC)。

## 技术栈一览

| 层 | 选型 |
|---|---|
| 桌面框架 | Tauri 2 |
| 后端 | Rust（`reqwest` + rustls、`serde`、`tokio`(test)） |
| 前端 | React 19 + TypeScript + Vite 7 |
| 存储格式 | YAML（case / 配置文件，规划中） |

## 仓库结构

```
apicase/
├── app/             # 应用代码（Tauri 项目）
├── docs/
│   ├── 0.latest/    # 当前全局最新文档（本目录，事实来源）
│   └── 1.feature/   # 各需求的产品技术方案
└── CLAUDE.md        # 全局提示词
```

## 文档索引

- [1.产品概念模型.md](1.产品概念模型.md) —— 定位、文件即数据、folder/case、关键设计决策。
- [2.技术架构.md](2.技术架构.md) —— 技术栈、目录结构、后端命令与数据模型、运行与构建。

## 路线（下一步）

1. **case 读写**：点击 `.yml` case 解析并渲染到请求视图、编辑后写回；folder/case 的新建与重命名。
2. 最近列表持久化、文件树外部变更自动刷新。
3. environment 多套配置切换、多节点 DAG 编排画布、历史 / 鉴权 / 导入导出、OpenAPI(SPEC)。

> 已完成：打开目录作为工作空间、左侧文件树（见 [1.feature/20260630-工作空间与界面外壳](../1.feature/20260630-工作空间与界面外壳/技术方案.md)）。

---

_最后更新：2026-06-30_
